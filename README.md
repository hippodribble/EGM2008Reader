# Download the EGM2008 16-bit PGM file from NASA or somewhere.
# Use the library in your code (you will need to pass the file location or use an environment variable
# Use the command line utility to create
* get the geoidal height for a single point
* get a list of heights from a grid specified by four corners and 2 spacings
* get a list of heights from a a list of positions specified in a CSV file
